import React from "react";

export const MultiInput = ({ text }: String | any) => {
  return (
    <div>
      <label className="text-gray-700 block mb-1 font-semibold">Label</label>
      <textarea className="form-textarea leading-5" />
    </div>
  );
};
